---
stages:
- name: Deploy assets
  properties:
  - name: foo
    value: bar
    type: text
  - name: chatbot_json_url
    value: '${CHATBOT_JSON_URL}'
  - name: chatbot_json_url_noquote
    value: ${CHATBOT_JSON_URL}
  jobs:
  - name: Deploy Chatbot Assetsx
    type: deployer
    target:
      region_id: ibm:yp:us-south
      organization: Developer Advocacy
      space: dev
      application: Load Chatbot Assets
    script: |-
      #!/bin/bash
      # FOO
      echo ${CHATBOT_JSON_URL}
hooks:
- enabled: true
  label: null
  ssl_enabled: false
  url: https://devops-api.ng.bluemix.net/v1/messaging/webhook/publish
